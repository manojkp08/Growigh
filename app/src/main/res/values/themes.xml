<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Growigh" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Growigh" parent="Base.Theme.Growigh">
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <style name="SearchBar" parent="Widget.AppCompat.SearchView">
        <item name="android:textColor">@color/black</item>
        <item name="android:background">@drawable/search_view_bg</item>
        <item name="hintTextColor">@color/black</item>
        <item name="hintAnimationEnabled">true</item>
        <item name="hintEnabled">true</item>
        <item name="defaultQueryHint">Explore</item>
        <item name="textAllCaps">false</item>
    </style>
</resources>